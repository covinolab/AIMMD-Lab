name: test
channels:

  - conda-forge
  - bioconda
  - defaults
dependencies:
    # Base depends
  - python
  - pip

 - tqdm
- numpy
- scipy
- torch
- matplotlib
- MDAnalysis
- mdtraj
- psutil

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - gromacs

    # Pip-only installs
  #- pip:
  #  - codecov
