name: test
channels:

  - conda-forge
  - bioconda
  - defaults
dependencies:
    # Base depends
  - python
  - pip

  - dill
  - tqdm
  - numpy
  - scipy
  - pytorch
  - matplotlib
  - MDAnalysis
  - mdtraj
  - psutil

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - gromacs

    # Pip-only installs
  #- pip:
  #  - codecov
