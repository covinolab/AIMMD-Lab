"""
aimmd.core
==========

Core subpackage for AIMMD.

This package contains low-level, broadly reusable components that other AIMMD
modules build upon (e.g., base classes, descriptors, and general utilities).

The file is intentionally minimal: it mainly exists to define the package and
to provide a natural import root (``aimmd.core``).
"""
